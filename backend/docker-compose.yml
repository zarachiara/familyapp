version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: familyflow-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: familyflow
      MONGO_INITDB_ROOT_PASSWORD: familyflow123
      MONGO_INITDB_DATABASE: familyflow
    volumes:
      - mongodb_data:/data/db
      - mongodb_config:/data/configdb
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/familyflow --quiet
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mongodb_data:
    driver: local
  mongodb_config:
    driver: local